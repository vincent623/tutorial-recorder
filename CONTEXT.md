# 教程自动录制

本上下文描述教程录制期间，浏览器页面事实如何被观察、授权给 AI，并转化为可审计的教程步骤。

## AI 浏览器操作

**浏览器观察（Browser Observation）**：
AI 录制在某一时刻对目标标签页可操作状态的隐私受限描述，用于动作决策、风险判断和结果验证。完整浏览器观察只在本地短暂存在，不属于教程内容。
_Avoid_: DOM 快照、页面抓取、截图分析结果

**观察周期（Observation Cycle）**：
为产生一次浏览器观察而同步获取干净截图、交互元素、观察能力和页面位置的短暂过程。观察周期不包含教程资产保存、远程模型调用或动作执行。
_Avoid_: Agent 步骤、截图保存、模型请求

**观察元素引用（Observation Element Reference）**：
浏览器观察中指向一个可交互元素的不透明标识，仅在产生它的那次观察内有效。动作执行前必须复验；页面变化或进入下一次观察后，不得继续信任原引用。
_Avoid_: 永久元素 ID、跨页面选择器、稳定坐标

**可交互目标（Interactive Target）**：
在当前观察窗口内可见、未被遮挡且能够通过可访问性语义或明确网页行为识别的操作对象。无法建立可靠网页语义的自绘区域不是可交互目标，只能进入视觉降级。
_Avoid_: 任意 DOM 节点、仅有坐标的区域、隐藏控件

**元素动作（Element Action）**：
通过观察元素引用指向可交互目标的 AI 动作。目标文字只用于说明和审计，不承担实际定位责任。
_Avoid_: 文字匹配动作、坐标点击

**视觉降级动作（Visual Fallback Action）**：
无法建立可靠观察元素引用时，基于决策截图坐标提出的受限动作。它必须声明降级原因并经过单次动作确认。
_Avoid_: 普通元素动作、静默坐标兜底

**观察能力（Observation Capability）**：
一次浏览器观察对主文档、Shadow DOM、iframe 等页面区域能够可靠识别和复验的范围。能力不足必须显式标记，并进入需要用户确认的视觉降级，而不能被当作与完整能力等价。
_Avoid_: adapter 完全等价、静默猜测、无差别页面支持

**观察结果（Observation Outcome）**：
浏览器观察的可用程度：`ready` 表示截图与结构事实一致，`degraded` 表示只能在声明的能力限制下继续，`unavailable` 表示不能开始远程决策。局部页面区域不可达属于降级，不等同于整个录制失败。
_Avoid_: 成功/异常二元状态、静默降级、拼接不同周期的事实

**观察收据（Observation Receipt）**：
随教程保存在本地、用于解释观察能力和失败原因的最小脱敏记录。它可以包含结果、能力、数量、耗时和降级原因，但不能包含元素文字清单、完整地址、输入值、DOM、决策截图或节点映射。
_Avoid_: 遥测、页面快照、完整运行日志

**观察窗口（Observation Window）**：
远程观察投影所覆盖的当前可见页面范围。投影只描述窗口内的交互元素；超出数量上限时必须声明截断，屏幕外内容需要滚动后重新观察。
_Avoid_: 整页观察、隐式截断、屏幕外元素清单

**观察细化（Observation Refinement）**：
当观察窗口内的元素因数量上限未进入远程观察投影时，对指定区域或元素类别重新产生更聚焦观察的过程。观察细化不修改页面，也不是视觉降级动作。
_Avoid_: 坐标兜底、页面缩放、重复模型猜测

**教程截图（Tutorial Screenshot）**：
作为教程资产保存和导出的干净页面图像，不包含仅供 AI 定位使用的元素编号。
_Avoid_: Agent 调试图、元素标注图

**决策截图（Decision Screenshot）**：
仅在一次远程观察请求期间短暂存在、以观察元素引用标注交互目标的图像。它不属于教程资产，不进入历史或发布证据。
_Avoid_: 教程截图、持久化标注图

**远程观察投影（Remote Observation Projection）**：
浏览器观察中经用户授权后可以发送给 AI 服务商的最小脱敏子集，仅包含截图和数量受限的可见交互元素描述；不包含输入值、隐藏元素、原始 HTML、Cookie、Storage 或完整 DOM。
_Avoid_: DOM 上传、页面全文、完整观察

**远程目标摘要（Remote Destination Summary）**：
远程观察投影中对链接或表单目标的脱敏描述，只表达同站/跨站、主机名、路径和请求方法。完整目标地址仅用于本地风险判断，query、hash、凭据和其他参数不得进入远程目标摘要。
_Avoid_: 完整链接、表单原始地址、URL 参数

**AI 数据共享授权（AI Data Sharing Consent）**：
用户对截图和远程观察投影发送给所选 AI 服务商的显式、可撤回许可。撤回后，不再允许新的远程观察请求继续或开始。
_Avoid_: API Key 已配置、默认同意、一次性永久授权

**单次动作确认（One-time Action Approval）**：
用户对当前页面语义下、针对特定目标的一次高影响或不确定动作的许可，而不是对固定屏幕坐标的许可。目标语义、风险或页面来源变化会使确认失效；同一目标仅发生位置变化时可以重新校准。
_Avoid_: 坐标授权、站点永久授权、同类动作批量授权
